export const scatterPlot = (parent, props) => {
  // unpack my props
  const {
    data,
    margin,
    xValue, 
    xAxisLabel,
    yValue, 
    yAxisLabel,
    circleRadius
  } = props;

  // Add your implementation here
  // ...

};
