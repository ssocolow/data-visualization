export const sizeLegend = (parent, props) => {
  // unpack my props
  const { 
    sizeScale, 
    numTicks,
    spacing,
    textOffset,
    circleFill
  } = props;

  // Write your code here
  // to create the "component"  

}
