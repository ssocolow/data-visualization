export const colourLegend = (parent, props) => {
  // unpack my props
  const { 
    colourScale, 
    circleRadius,
    spacing,
    textOffset
  } = props;

  // Write your code here
  // ...

}
